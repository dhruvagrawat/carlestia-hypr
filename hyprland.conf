$hypr = ~/.config/hypr
$hl = $hypr/hyprland
$cConf = ~/.config/caelestia

# Variables (colours + other vars)
exec = cp -L --no-preserve=mode --update=none $hypr/scheme/default.conf $hypr/scheme/current.conf
source = $hypr/scheme/current.conf
source = $hypr/variables.conf

# User variables
exec = mkdir -p $cConf && touch -a $cConf/hypr-vars.conf


# Default monitor conf
# =========================
# LG Ultrawide HDMI-A-1 @75Hz
# =========================

# Normal (default landscape)
# monitor = HDMI-A-1, 2560x1080@74.99, 0x0, 1, transform, 0

# Rotated 90° clockwise (portrait right)
#  monitor = HDMI-A-1, 2560x1080@74.99, 0x0, 1, transform, 1

# Rotated 180° (upside down)
# monitor = HDMI-A-1, 2560x1080@74.99, 0x0, 1, transform, 2

# Rotated 90° counter-clockwise (portrait left)
monitor = HDMI-A-1, 2560x1080@74.99, 0x0, 1, transform, 3


# Secondary: Laptop screen (to the right of LG)
monitor = eDP-1, 1920x1080@59.98, 2560x0, 1


# Configs
source = $hl/env.conf
source = $hl/general.conf
source = $hl/input.conf
source = $hl/misc.conf
source = $hl/animations.conf
source = $hl/decoration.conf
source = $hl/group.conf
source = $hl/execs.conf
source = $hl/rules.conf
source = $hl/gestures.conf
source = $hl/keybinds.conf

# User configs
exec = mkdir -p $cConf && touch -a $cConf/hypr-user.conf
source = $cConf/hypr-user.conf
